# docparse_web — Cohere-only (Parse + command-a) web version of docparse.
#
# The user supplies their OWN Cohere API key in the browser. It is never read
# from this env file, never stored server-side, and never logged. Set nothing
# here; this file exists only to document that fact.

COHERE_API_KEY=
